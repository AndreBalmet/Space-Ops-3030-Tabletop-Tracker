{
  "rules": {
    "gameData": {
      ".read": true,
      ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
    },
    "gameData_backup": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
      ".write": "auth != null && root.child('admins').child(auth.uid).val() === true"
    },

    "teams": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && auth.token.email_verified === true"
      }
    },
    "deletedTeams": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && auth.token.email_verified === true"
      }
    },

    "users": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "usernames": {
      "playtest1": { ".read": true },
      "playtest2": { ".read": true },
      "playtest3": { ".read": true },
      "playtest4": { ".read": true },
      "$name": {
        ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
        ".write": "auth != null && ((!data.exists() && newData.child('uid').val() === auth.uid) || (data.child('uid').val() === auth.uid && !newData.exists()))"
      }
    },

    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid"
      }
    },

    "players": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
      ".write": false
    },

    "audit": {
      ".read": "auth != null && root.child('admins').child(auth.uid).val() === true",
      "$entry": {
        ".write": "auth != null && !data.exists() && newData.child('uid').val() === auth.uid && auth.token.email_verified === true"
      }
    },

    "sessions":  { ".read": false, ".write": false },
    "templates": { ".read": false, ".write": false },
    "admin":     { ".read": false, ".write": false }
  }
}
